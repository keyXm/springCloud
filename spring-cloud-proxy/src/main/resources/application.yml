eureka:
  instance:
    hostname: localhost
    instance-id: http://localhost:8083
  client:
    serviceUrl:
      defaultZone: http://localhost:8080/eureka/
spring:
  application:
    name: gateway
server:
  port: 8083
zuul:
  routes:
    wechat: /wechat/**

